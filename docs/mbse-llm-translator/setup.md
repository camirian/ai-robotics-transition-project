# MBSE-to-LLM Translator Setup

## Prerequisites
- Python 3.10
- Docker Desktop
- AWS account

## Installation
1. Clone: `git clone ...`
2. Install: `pip install -r requirements.txt`
3. Build Docker: `docker build -t mbse-llm-translator .`
4. Run: `docker run -p 5000:5000 mbse-llm-translator`